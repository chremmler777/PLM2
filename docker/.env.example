# Production environment — copy to .env and fill in.

# REQUIRED: JWT signing key. Generate with:  openssl rand -hex 32
SECRET_KEY=

# REQUIRED: PostgreSQL password
POSTGRES_PASSWORD=

# Optional
REDIS_PASSWORD=plm
HTTP_PORT=80
# JSON list of allowed browser origins (the public URL of the frontend)
CORS_ORIGINS=["http://localhost"]
